Directory contains sample for dumping (topic, payload) pairs (e.g. MQTT) in a functional style.

Build with

```bash
cmake -B build-mqtt_dump -S mqtt_dump
cmake --build build-mqtt_dump -j16
```

commands.
